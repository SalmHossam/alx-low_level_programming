#include "main.h"
#include <stdio.h>

/**
 * _isupper check if the digit id upper case or not
 *
 * Return 1 for uppercase else Return 0
 */

int _isupper (int c)
{

	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
